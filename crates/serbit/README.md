process:

 - get chew done / offsets with no overhead where possible
 - get the readers done for items
 - get the stages & seqs generation done

Semantic analysis on ir + documentation

combi based proc_macro frontend
make easy methods for adding other frontends
make the generated documentation nice (generate svg) 
